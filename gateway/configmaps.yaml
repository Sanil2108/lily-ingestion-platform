# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: kong-config
#   namespace: kong
# data:
#   kong.conf: |
#     database = off
#     declarative_config = /kong/declarative/kong.yml
#     plugins = bundled, custom-auth-plugin
#     nginx_proxy_real_ip_header = X-Forwarded-For
# ---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kong-config
  namespace: kong
data:
  kong.yaml: |
    _format_version: "1.1"
    services:
    - name: example-service
      url: http://example.com
      routes:
      - name: example-route
        paths:
        - /
